[project]
name = "rag-agent-course-project"
version = "0.1.0"
description = "Educational project for teaching RAG agents end-to-end."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
pythonpath = ["src"]
